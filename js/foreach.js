//a callbackfunction its a function to execute for each element in the array
//a callback is a function passed as an argument to another function.
 let arr=[1,2,3,4,5,"delhi"];

 arr.forEach((val) =>{
   console.log(val.toUpperCase(),idx);

 });